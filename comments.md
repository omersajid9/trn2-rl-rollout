1. HBM Mini-verl’s generation requires ∼15GB HBM during compilation as opposed to vLLM’s 0.45GB. 2. DRAM vLLM requires way more DRAM during compilation and generation than mini-verl." This seems to indicate a huge memory consumption triggered during compilation/recompilation on Trainium. Given the similar speed between vllm (likely eager mode without CUDA cuda like on NVIDIA) vs. mini-verl under warmp runs, this memory consumption might become a major bottleneck and probably requires more careful management especially when recompilation might need to be frequently triggered due to variable sequence lengths.
2. [4:49 PM]2. Related to the comment above, do you know how much overhead the system pays for maintaining the compiled code/graph? E.g., whether the 15GB memory is just a transient memory allocation or a subset of it remains occupying the working set memory? If so, how much that memory consumption is.
3. [4:51 PM]3. Can we cache multiple compiled code/graph for variable sequence lengths? Related to the issue above, if per compilation leads to large working-set memory consumption that persists during the actual model inference, then we may not cache too many compiled graph (need to check if such mechanism of caching multiple compiled graph/code exists already).
4. [4:54 PM]4. Related to the above, this remains the TLT work, which maintains a memory-efficient pool of pre-captured CUDA graphs for variable input batches for accelerating RL: https://arxiv.org/pdf/2511.16665. Maybe we have similar opportunity as we may need different mechanism from CUDA graph to build such memory-efficient pre-captured compilation graphs on Trainium. (edi
5. I'm a bit surprised by this "Recompilation is keyed by chunk count" . Do you know if running vLLM on NVIDIA GPU with chunked prefill enabled, does it also require to capture different CUDA graphs that depend on the chunk count? The chunked prefill introduces loops, which I thought we only need to capture the CUDA graph once for each chunk size. Can you check and confirm if that's the case? Assume CUDA graph is not keyed by chunk count, then perhaps it is a Trainium thing that recompilation is keyed by chunk count. Prefill lengths vary significantly in practice. If recompilation is triggered by different chunk count (the number of chunks), that seems to add a lot of overhead.

Thanks so much for running these experiments! Here are a few quick comments. I will take a closer look later.

mini-verl has enabled layer-wise compilation and flash-attn. They will reduce compilation time and improve chunk prefill speed respectively.
Chunk prefill shouldn't trigger re-compilation among chunks because the computation shapes are the same. This is probably a bug and I will look into it.
Mini-ver has supported TP=4, 16 for Qwen3-8B.
